# 提示词 17 — TDD 模式

> 对应 Skill：`tdd-mode`
> 本模式不替代治理总控；它只把当前任务固定为 **测试驱动交付**。

## 目标

先把验收标准转成测试点、失败信号和回归断言，再做最小实现。

## 默认顺序

1. 明确可验证的验收标准
2. 先列要新增 / 修改的测试点或断言
3. 说明当前应出现的失败信号
4. 给出最小实现路径
5. 完成后进入评测门禁

## 约束

- 不要先写实现、后补测试
- 不要在验收标准还模糊时强行进入 TDD
- 若出现公共接口、数据库结构或跨服务取舍，切回 `acd-mode`
- 若证据不足、现象不清，切回 `aod-mode`

## 输出协议

```markdown
## TDD 模式
- 验收标准：
- 测试点 / 断言：
- 失败信号：
- 最小实现路径：
- 回归验证：
```
